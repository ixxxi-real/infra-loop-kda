# Security policy

Do not put credentials, checkpoint paths, gateway addresses, GPU UUIDs, raw
profiler reports or agent transcripts in an issue or pull request. Store raw
runtime evidence outside Git and commit only redacted manifests and checksums.

For a private report, contact the repository owner through the GitHub security
contact configured for the published repository. Include the affected commit,
the command that exposed the problem and a sanitized reproduction.
