# Kimi K3 decode

Decode is the next Kimi K3 optimization stage. It has no accepted kernel or
benchmark result yet.

When work starts, add one directory per kernel below kernels/, put the contract
and source provenance in design/, and keep raw runs in the ignored runtime
area. Register the active task in config/project.local.json only after its
workload and acceptance gates are real.
