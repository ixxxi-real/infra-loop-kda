diff --git a/python/sglang/kernels/ops/attention/fla/chunk_delta_h.py b/python/sglang/kernels/ops/attention/fla/chunk_delta_h.py
index 84ae081f3..42dda37cd 100644
--- a/python/sglang/kernels/ops/attention/fla/chunk_delta_h.py
+++ b/python/sglang/kernels/ops/attention/fla/chunk_delta_h.py
@@ -98,14 +98,18 @@ def chunk_gated_delta_rule_fwd_kernel_h_blockdim64(
         NT = tl.cdiv(T, BT)
         boh = i_n * NT
 
-    # [BV, BK]
-    b_h1 = tl.zeros([BV, 64], dtype=tl.float32)
+    # [BK, BV]: state is held K-major so both loop matmuls consume it directly.
+    # b_v = dot(b_w [BT,64], b_h [64,BV]) and b_h += dot(b_k [64,BT], b_v [BT,BV])
+    # are then transpose-free; the V-major form needed a tl.trans on each side.
+    # Memory layout of h / initial_state / track_state is unchanged -- only the
+    # block_ptr view below transposes.
+    b_h1 = tl.zeros([64, BV], dtype=tl.float32)
     if K > 64:
-        b_h2 = tl.zeros([BV, 64], dtype=tl.float32)
+        b_h2 = tl.zeros([64, BV], dtype=tl.float32)
     if K > 128:
-        b_h3 = tl.zeros([BV, 64], dtype=tl.float32)
+        b_h3 = tl.zeros([64, BV], dtype=tl.float32)
     if K > 192:
-        b_h4 = tl.zeros([BV, 64], dtype=tl.float32)
+        b_h4 = tl.zeros([64, BV], dtype=tl.float32)
 
     # calculate offset
     h += ((boh * H + i_h) * V * K).to(tl.int64)
@@ -145,64 +149,64 @@ def chunk_gated_delta_rule_fwd_kernel_h_blockdim64(
 
     # load initial state
     if USE_INITIAL_STATE and valid_state:
-        p_h0_1 = tl.make_block_ptr(h0, (V, K), (K, 1), (i_v * BV, 0), (BV, 64), (1, 0))
+        p_h0_1 = tl.make_block_ptr(h0, (K, V), (1, K), (0, i_v * BV), (64, BV), (0, 1))
         b_h1 += tl.load(p_h0_1, boundary_check=(0, 1)).to(tl.float32)
         if K > 64:
             p_h0_2 = tl.make_block_ptr(
-                h0, (V, K), (K, 1), (i_v * BV, 64), (BV, 64), (1, 0)
+                h0, (K, V), (1, K), (64, i_v * BV), (64, BV), (0, 1)
             )
             b_h2 += tl.load(p_h0_2, boundary_check=(0, 1)).to(tl.float32)
         if K > 128:
             p_h0_3 = tl.make_block_ptr(
-                h0, (V, K), (K, 1), (i_v * BV, 128), (BV, 64), (1, 0)
+                h0, (K, V), (1, K), (128, i_v * BV), (64, BV), (0, 1)
             )
             b_h3 += tl.load(p_h0_3, boundary_check=(0, 1)).to(tl.float32)
         if K > 192:
             p_h0_4 = tl.make_block_ptr(
-                h0, (V, K), (K, 1), (i_v * BV, 192), (BV, 64), (1, 0)
+                h0, (K, V), (1, K), (192, i_v * BV), (64, BV), (0, 1)
             )
             b_h4 += tl.load(p_h0_4, boundary_check=(0, 1)).to(tl.float32)
 
     # main recurrence
     for i_t in range(NT):
         p_h1 = tl.make_block_ptr(
-            h + i_t * stride_h, (V, K), (K, 1), (i_v * BV, 0), (BV, 64), (1, 0)
+            h + i_t * stride_h, (K, V), (1, K), (0, i_v * BV), (64, BV), (0, 1)
         )
         tl.store(p_h1, b_h1.to(p_h1.dtype.element_ty), boundary_check=(0, 1))
         if K > 64:
             p_h2 = tl.make_block_ptr(
-                h + i_t * stride_h, (V, K), (K, 1), (i_v * BV, 64), (BV, 64), (1, 0)
+                h + i_t * stride_h, (K, V), (1, K), (64, i_v * BV), (64, BV), (0, 1)
             )
             tl.store(p_h2, b_h2.to(p_h2.dtype.element_ty), boundary_check=(0, 1))
         if K > 128:
             p_h3 = tl.make_block_ptr(
-                h + i_t * stride_h, (V, K), (K, 1), (i_v * BV, 128), (BV, 64), (1, 0)
+                h + i_t * stride_h, (K, V), (1, K), (128, i_v * BV), (64, BV), (0, 1)
             )
             tl.store(p_h3, b_h3.to(p_h3.dtype.element_ty), boundary_check=(0, 1))
         if K > 192:
             p_h4 = tl.make_block_ptr(
-                h + i_t * stride_h, (V, K), (K, 1), (i_v * BV, 192), (BV, 64), (1, 0)
+                h + i_t * stride_h, (K, V), (1, K), (192, i_v * BV), (64, BV), (0, 1)
             )
             tl.store(p_h4, b_h4.to(p_h4.dtype.element_ty), boundary_check=(0, 1))
 
         if TRACK_STATE and i_t == i_track:
             p_t1 = tl.make_block_ptr(
-                p_track_base, (V, K), (K, 1), (i_v * BV, 0), (BV, 64), (1, 0)
+                p_track_base, (K, V), (1, K), (0, i_v * BV), (64, BV), (0, 1)
             )
             tl.store(p_t1, b_h1, boundary_check=(0, 1))
             if K > 64:
                 p_t2 = tl.make_block_ptr(
-                    p_track_base, (V, K), (K, 1), (i_v * BV, 64), (BV, 64), (1, 0)
+                    p_track_base, (K, V), (1, K), (64, i_v * BV), (64, BV), (0, 1)
                 )
                 tl.store(p_t2, b_h2, boundary_check=(0, 1))
             if K > 128:
                 p_t3 = tl.make_block_ptr(
-                    p_track_base, (V, K), (K, 1), (i_v * BV, 128), (BV, 64), (1, 0)
+                    p_track_base, (K, V), (1, K), (128, i_v * BV), (64, BV), (0, 1)
                 )
                 tl.store(p_t3, b_h3, boundary_check=(0, 1))
             if K > 192:
                 p_t4 = tl.make_block_ptr(
-                    p_track_base, (V, K), (K, 1), (i_v * BV, 192), (BV, 64), (1, 0)
+                    p_track_base, (K, V), (1, K), (192, i_v * BV), (64, BV), (0, 1)
                 )
                 tl.store(p_t4, b_h4, boundary_check=(0, 1))
 
@@ -210,25 +214,26 @@ def chunk_gated_delta_rule_fwd_kernel_h_blockdim64(
             w, (T, K), (stride_w, 1), (i_t * BT, 0), (BT, 64), (1, 0)
         )
         b_w = tl.load(p_w, boundary_check=(0, 1))
-        b_v = tl.dot(b_w, tl.trans(b_h1).to(b_w.dtype))
+        # K-major state feeds tl.dot directly: [BT,64] x [64,BV] -> [BT,BV].
+        b_v = tl.dot(b_w, b_h1.to(b_w.dtype))
         if K > 64:
             p_w = tl.make_block_ptr(
                 w, (T, K), (stride_w, 1), (i_t * BT, 64), (BT, 64), (1, 0)
             )
             b_w = tl.load(p_w, boundary_check=(0, 1))
-            b_v += tl.dot(b_w, tl.trans(b_h2).to(b_w.dtype))
+            b_v += tl.dot(b_w, b_h2.to(b_w.dtype))
         if K > 128:
             p_w = tl.make_block_ptr(
                 w, (T, K), (stride_w, 1), (i_t * BT, 128), (BT, 64), (1, 0)
             )
             b_w = tl.load(p_w, boundary_check=(0, 1))
-            b_v += tl.dot(b_w, tl.trans(b_h3).to(b_w.dtype))
+            b_v += tl.dot(b_w, b_h3.to(b_w.dtype))
         if K > 192:
             p_w = tl.make_block_ptr(
                 w, (T, K), (stride_w, 1), (i_t * BT, 192), (BT, 64), (1, 0)
             )
             b_w = tl.load(p_w, boundary_check=(0, 1))
-            b_v += tl.dot(b_w, tl.trans(b_h4).to(b_w.dtype))
+            b_v += tl.dot(b_w, b_h4.to(b_w.dtype))
         p_v = tl.make_block_ptr(
             v, (T, V), (stride_v, 1), (i_t * BT, i_v * BV), (BT, BV), (1, 0)
         )
@@ -264,10 +269,12 @@ def chunk_gated_delta_rule_fwd_kernel_h_blockdim64(
                 mask=(o_k1 < K),
                 other=0.0,
             )
+            # K is dim 0 of the K-major state, so the per-channel gate
+            # broadcasts along dim 1 ([:, None]), not dim 0.
             if USE_EXP2:
-                b_h1 *= exp2(b_gk_last1)[None, :]
+                b_h1 *= exp2(b_gk_last1)[:, None]
             else:
-                b_h1 *= exp(b_gk_last1)[None, :]
+                b_h1 *= exp(b_gk_last1)[:, None]
             if K > 64:
                 o_k2 = 64 + o_k1
                 b_gk_last2 = tl.load(
@@ -276,9 +283,9 @@ def chunk_gated_delta_rule_fwd_kernel_h_blockdim64(
                     other=0.0,
                 )
                 if USE_EXP2:
-                    b_h2 *= exp2(b_gk_last2)[None, :]
+                    b_h2 *= exp2(b_gk_last2)[:, None]
                 else:
-                    b_h2 *= exp(b_gk_last2)[None, :]
+                    b_h2 *= exp(b_gk_last2)[:, None]
             if K > 128:
                 o_k3 = 128 + o_k1
                 b_gk_last3 = tl.load(
@@ -287,9 +294,9 @@ def chunk_gated_delta_rule_fwd_kernel_h_blockdim64(
                     other=0.0,
                 )
                 if USE_EXP2:
-                    b_h3 *= exp2(b_gk_last3)[None, :]
+                    b_h3 *= exp2(b_gk_last3)[:, None]
                 else:
-                    b_h3 *= exp(b_gk_last3)[None, :]
+                    b_h3 *= exp(b_gk_last3)[:, None]
             if K > 192:
                 o_k4 = 192 + o_k1
                 b_gk_last4 = tl.load(
@@ -298,52 +305,54 @@ def chunk_gated_delta_rule_fwd_kernel_h_blockdim64(
                     other=0.0,
                 )
                 if USE_EXP2:
-                    b_h4 *= exp2(b_gk_last4)[None, :]
+                    b_h4 *= exp2(b_gk_last4)[:, None]
                 else:
-                    b_h4 *= exp(b_gk_last4)[None, :]
+                    b_h4 *= exp(b_gk_last4)[:, None]
         b_v = b_v.to(k.dtype.element_ty)
 
         p_k = tl.make_block_ptr(
             k, (K, T), (1, stride_k), (0, i_t * BT), (64, BT), (0, 1)
         )
         b_k = tl.load(p_k, boundary_check=(0, 1))
-        b_h1 += tl.trans(tl.dot(b_k, b_v))
+        # dot([64,BT], [BT,BV]) -> [64,BV] already matches the K-major
+        # accumulator, so the outer tl.trans the V-major form needed is gone.
+        b_h1 += tl.dot(b_k, b_v)
         if K > 64:
             p_k = tl.make_block_ptr(
                 k, (K, T), (1, stride_k), (64, i_t * BT), (64, BT), (0, 1)
             )
             b_k = tl.load(p_k, boundary_check=(0, 1))
-            b_h2 += tl.trans(tl.dot(b_k, b_v))
+            b_h2 += tl.dot(b_k, b_v)
         if K > 128:
             p_k = tl.make_block_ptr(
                 k, (K, T), (1, stride_k), (128, i_t * BT), (64, BT), (0, 1)
             )
             b_k = tl.load(p_k, boundary_check=(0, 1))
-            b_h3 += tl.trans(tl.dot(b_k, b_v))
+            b_h3 += tl.dot(b_k, b_v)
         if K > 192:
             p_k = tl.make_block_ptr(
                 k, (K, T), (1, stride_k), (192, i_t * BT), (64, BT), (0, 1)
             )
             b_k = tl.load(p_k, boundary_check=(0, 1))
-            b_h4 += tl.trans(tl.dot(b_k, b_v))
+            b_h4 += tl.dot(b_k, b_v)
 
     # epilogue
     if INPLACE_UPDATE and valid_state:
-        p_ht = tl.make_block_ptr(ht, (V, K), (K, 1), (i_v * BV, 0), (BV, 64), (1, 0))
+        p_ht = tl.make_block_ptr(ht, (K, V), (1, K), (0, i_v * BV), (64, BV), (0, 1))
         tl.store(p_ht, b_h1.to(p_ht.dtype.element_ty), boundary_check=(0, 1))
         if K > 64:
             p_ht = tl.make_block_ptr(
-                ht, (V, K), (K, 1), (i_v * BV, 64), (BV, 64), (1, 0)
+                ht, (K, V), (1, K), (64, i_v * BV), (64, BV), (0, 1)
             )
             tl.store(p_ht, b_h2.to(p_ht.dtype.element_ty), boundary_check=(0, 1))
         if K > 128:
             p_ht = tl.make_block_ptr(
-                ht, (V, K), (K, 1), (i_v * BV, 128), (BV, 64), (1, 0)
+                ht, (K, V), (1, K), (128, i_v * BV), (64, BV), (0, 1)
             )
             tl.store(p_ht, b_h3.to(p_ht.dtype.element_ty), boundary_check=(0, 1))
         if K > 192:
             p_ht = tl.make_block_ptr(
-                ht, (V, K), (K, 1), (i_v * BV, 192), (BV, 64), (1, 0)
+                ht, (K, V), (1, K), (192, i_v * BV), (64, BV), (0, 1)
             )
             tl.store(p_ht, b_h4.to(p_ht.dtype.element_ty), boundary_check=(0, 1))
 
